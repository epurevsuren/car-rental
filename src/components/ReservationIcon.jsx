import { Link } from 'react-router-dom';

const ReservationIcon = () => (
  <Link to="/reservation" className="text-blue-600 font-semibold hover:underline">
    📋 Reservation
  </Link>
);

export default ReservationIcon;
